services:
  - type: web
    name: ipl-prediction-api
    runtime: python
    dockerfilePath: Dockerfile
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.8"
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        value: "sqlite:///./ipl_prediction.db"
